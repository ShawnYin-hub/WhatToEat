# Android 应用完整部署指南

## 📱 目录
1. [Android Studio 测试](#android-studio-测试)
2. [生成签名密钥](#生成签名密钥)
3. [构建发布版本APK](#构建发布版本apk)
4. [上架到Google Play](#上架到google-play)
5. [上架到其他应用商店](#上架到其他应用商店)

---

## 🧪 Android Studio 测试

### 当前状态
✅ Android Studio 已打开项目（在 `android/` 目录）

### 在模拟器或真机上测试

#### 方法1：使用Android模拟器
1. 在 Android Studio 顶部工具栏点击 **Device Manager**
2. 点击 **Create Device**
3. 选择设备型号（推荐：Pixel 5 或 Pixel 6）
4. 选择系统镜像（推荐：API 33 或 API 34）
5. 点击 **Finish** 创建模拟器
6. 点击绿色的 **Run** 按钮（或按 Shift+F10）
7. 等待应用安装并启动

#### 方法2：使用真实Android手机
1. 在手机上启用**开发者选项**：
   - 进入 **设置** > **关于手机**
   - 连续点击 **版本号** 7次
2. 启用 **USB调试**：
   - 返回 **设置** > **开发者选项**
   - 开启 **USB调试**
3. 用USB连接手机到电脑
4. 手机上允许USB调试
5. 在Android Studio中选择你的设备
6. 点击 **Run** 按钮

### 测试重点功能
- [ ] 应用正常启动
- [ ] 界面显示正常
- [ ] 地图服务选择功能
- [ ] 位置获取功能
- [ ] 菜系选择功能
- [ ] "帮我选"功能
- [ ] 登录/注册功能（需要配置Supabase）
- [ ] 头像上传功能（需要登录）
- [ ] 中英文切换

---

## 🔑 生成签名密钥

### 为什么需要签名？
- Google Play要求所有APK必须签名
- 用于验证应用的真实性和完整性
- 保证应用更新来自同一开发者

### 生成密钥库（Keystore）

#### 步骤1：使用keytool生成密钥
打开命令行（在项目根目录），执行：

```bash
# Windows
keytool -genkey -v -keystore my-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias my-key-alias

# Mac/Linux
keytool -genkey -v -keystore my-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias my-key-alias
```

#### 步骤2：填写密钥信息
系统会提示你输入以下信息：

```
Enter keystore password: [输入密码，至少6个字符]
Re-enter new password: [再次输入密码]

What is your first and last name?
  [Unknown]:  Zhang San

What is the name of your organizational unit?
  [Unknown]:  Development

What is the name of your organization?
  [Unknown]:  WhatToEat

What is the name of your City or Locality?
  [Unknown]:  Beijing

What is the name of your State or Province?
  [Unknown]:  Beijing

What is the two-letter country code for this unit?
  [Unknown]:  CN

Is CN=Zhang San, OU=Development, O=WhatToEat, L=Beijing, ST=Beijing, C=CN correct?
  [no]:  yes

Enter key password for <my-key-alias>
        (RETURN if same as keystore password): [直接回车或输入新密码]
```

**重要提示**：
- ⚠️ **务必记住你的密码！**丢失密码将无法更新应用！
- ⚠️ **妥善保管 `my-release-key.jks` 文件！**建议备份到安全的地方
- ⚠️ **不要把密钥文件提交到Git！**

#### 步骤3：配置Android项目

创建 `android/keystore.properties` 文件：

```properties
storePassword=你的密钥库密码
keyPassword=你的密钥密码
keyAlias=my-key-alias
storeFile=../my-release-key.jks
```

将密钥文件移动到项目根目录：
```bash
mv my-release-key.jks D:/APP/what-to-eat-today/
```

#### 步骤4：修改 `android/app/build.gradle`

在文件开头添加（在 `android {` 之前）：

```gradle
def keystorePropertiesFile = rootProject.file("keystore.properties")
def keystoreProperties = new Properties()
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}
```

在 `android {` 块中添加：

```gradle
android {
    // ... 其他配置 ...
    
    signingConfigs {
        release {
            if (keystorePropertiesFile.exists()) {
                keyAlias keystoreProperties['keyAlias']
                keyPassword keystoreProperties['keyPassword']
                storeFile file(keystoreProperties['storeFile'])
                storePassword keystoreProperties['storePassword']
            }
        }
    }
    
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
```

---

## 📦 构建发布版本APK

### 方法1：使用Android Studio（推荐）

1. 在Android Studio中打开项目
2. 点击菜单 **Build** > **Generate Signed Bundle / APK**
3. 选择 **APK** 并点击 **Next**
4. 填写密钥信息：
   - **Key store path**: 选择 `my-release-key.jks`
   - **Key store password**: 输入密钥库密码
   - **Key alias**: my-key-alias
   - **Key password**: 输入密钥密码
5. 点击 **Next**
6. 选择 **release** 构建类型
7. 点击 **Finish**
8. 等待构建完成
9. APK位置：`android/app/release/app-release.apk`

### 方法2：使用命令行

```bash
# 进入android目录
cd android

# Windows
gradlew assembleRelease

# Mac/Linux
./gradlew assembleRelease
```

构建成功后，APK位置：
```
android/app/build/outputs/apk/release/app-release.apk
```

### 方法3：使用npm脚本（快速构建）

在项目根目录执行：

```bash
# 1. 构建Web应用
npm run build

# 2. 同步到Android
npx cap sync android

# 3. 构建APK
cd android && gradlew assembleRelease
```

---

## 🚀 上架到Google Play

### 前期准备

#### 1. 创建Google Play开发者账号
- 访问 [Google Play Console](https://play.google.com/console)
- 支付一次性注册费：$25 USD
- 填写开发者信息
- 等待审核（通常1-2天）

#### 2. 准备应用资源

##### 应用图标（已有）
- ✅ `android/app/src/main/res/mipmap-*/ic_launcher.png`

##### 应用截图（必需）
需要准备至少2张截图，推荐4-8张：
- **尺寸要求**：最小边320px，最大边3840px
- **格式**：JPG 或 PNG（24位，无透明度）
- **推荐尺寸**：1080x1920（竖屏）或 1920x1080（横屏）

**建议截图内容**：
1. 主页（地图服务选择）
2. 位置选择界面
3. 菜系选择界面
4. 随机结果展示
5. 个人中心页面
6. 每日推荐页面

##### 功能图片（Feature Graphic）
- **尺寸**：1024x500 像素
- **格式**：JPG 或 PNG（24位，无透明度）
- **用途**：在Google Play首页展示

##### 应用描述文本

**简短描述**（最多80字符）：
```
今天吃什么？AI帮你决定！解决选择困难症的美食助手
```

**完整描述**（最多4000字符）：
```
🍽️ 今天吃什么 - 你的AI美食决策助手

【核心功能】
✨ 智能推荐：基于位置、菜系偏好和距离智能推荐餐厅
🎰 随机抽选：选择困难症？让AI帮你做决定
🗺️ 多地图支持：高德地图（中国）、GreenStreet（全球）
📍 精准定位：自动或手动选择位置
🌏 双语支持：中文/English无缝切换

【特色功能】
💝 暖心饭点：每日AI推荐，午餐/下午茶/晚餐灵感
👤 用户画像：AI分析你的美食偏好，生成专属画像
📊 历史记录：记录你的美食足迹
🔔 定时提醒：饭点智能提醒

【为什么选择我们】
- 🎯 解决选择困难：不再纠结吃什么
- 🌟 个性化推荐：越用越懂你
- 🆓 完全免费：无需付费，无广告
- 🔒 隐私保护：数据安全有保障

【适用场景】
- 上班族午餐决策
- 朋友聚餐选店
- 探索新口味
- 旅行寻找美食

【支持的菜系】
川湘菜、粤菜、江浙菜、西北菜、日料、韩餐、西餐、意餐、泰餐、沙拉、汉堡、麻辣烫、烧烤、火锅、奶茶、咖啡等

立即下载，让AI帮你决定今天吃什么！
```

#### 3. 应用分类和标签
- **应用类型**：应用
- **类别**：美食佳饮（Food & Drink）
- **标签**：美食、餐厅、推荐、AI、决策

#### 4. 内容分级
通过Google Play的内容分级问卷：
- 应用不包含暴力内容
- 应用不包含成人内容
- 应用不针对儿童
- 年龄分级：3+ 或 Everyone

#### 5. 隐私政策
需要提供隐私政策URL。创建一个简单的隐私政策页面，包含：
- 收集的数据类型（位置、邮箱、搜索历史）
- 数据用途（推荐、账号管理）
- 数据存储（Supabase）
- 用户权利（查看、删除数据）

### 创建应用发布

#### 步骤1：创建新应用
1. 登录 [Google Play Console](https://play.google.com/console)
2. 点击 **创建应用**
3. 填写应用详情：
   - **应用名称**：今天吃什么
   - **默认语言**：中文（简体）
   - **应用类型**：应用
   - **免费/付费**：免费

#### 步骤2：完成应用设置
1. **应用访问权限**：
   - 选择"所有功能均可供所有用户使用，无需特殊访问权限"

2. **广告**：
   - 选择"否，我的应用不包含广告"

3. **内容分级**：
   - 填写问卷并提交

4. **目标受众群体和内容**：
   - 目标年龄：13岁及以上
   - 应用商店展示：仅限成年人

5. **新闻应用**：选择"否"

6. **新冠病毒追踪和状态应用**：选择"否"

7. **数据安全**：
   - 填写数据收集和共享详情
   - 上传隐私政策

#### 步骤3：上传APK/AAB

**推荐：生成AAB（Android App Bundle）**

AAB比APK更优，因为：
- 文件更小
- Google Play自动优化
- 强制要求（2021年8月起新应用）

生成AAB：
```bash
cd android
./gradlew bundleRelease
```

输出位置：`android/app/build/outputs/bundle/release/app-release.aab`

在Google Play Console中：
1. 进入 **生产** > **发布**
2. 点击 **创建新版本**
3. 上传 `app-release.aab`
4. 填写 **版本说明**：
   ```
   🎉 首次发布
   - AI智能推荐餐厅
   - 多菜系选择
   - 双语支持（中文/English）
   - 暖心饭点提醒
   ```
5. 点击 **保存** > **审核版本**

#### 步骤4：上传商店展示资源
1. 进入 **商店展示** > **主要商店展示信息**
2. 上传：
   - 应用图标（512x512）
   - 功能图片（1024x500）
   - 手机截图（至少2张）
3. 填写应用描述
4. 选择应用类别

#### 步骤5：设置定价和分发
1. 进入 **定价和分发**
2. 选择 **免费**
3. 选择分发国家/地区（建议全球）
4. 确认内容分级和隐私政策

#### 步骤6：提交审核
1. 确认所有必填项已完成（绿色对勾）
2. 点击 **发布到正式版**
3. 等待审核（通常1-3天）

### 审核后
- ✅ 审核通过：应用将在24小时内上架
- ❌ 审核被拒：查看原因，修改后重新提交

---

## 🏪 上架到其他应用商店

### 1. 华为应用市场（AppGallery）

#### 注册开发者账号
- 访问 [华为开发者联盟](https://developer.huawei.com/consumer/cn/)
- 注册并实名认证（企业：300元/年，个人免费）

#### 上传应用
1. 登录 [AppGallery Connect](https://developer.huawei.com/consumer/cn/service/josp/agc/index.html)
2. 点击 **我的应用** > **新建**
3. 填写应用信息
4. 上传APK（`app-release.apk`）
5. 上传截图和描述
6. 提交审核（通常1-3个工作日）

**特殊要求**：
- 需要软著（软件著作权）或ICP备案（企业）
- 个人开发者可能需要提供更多资料

### 2. 小米应用商店

#### 注册开发者账号
- 访问 [小米开放平台](https://dev.mi.com/)
- 注册并实名认证（免费）

#### 上传应用
1. 登录 [小米开发者中心](https://dev.mi.com/console/)
2. 创建新应用
3. 上传APK
4. 填写应用信息和截图
5. 提交审核（通常1-2个工作日）

### 3. OPPO软件商店

#### 注册开发者账号
- 访问 [OPPO开放平台](https://open.oppomobile.com/)
- 注册并认证

#### 上传应用
1. 登录开发者平台
2. 创建应用
3. 上传APK和资源
4. 提交审核

### 4. vivo应用商店

#### 注册开发者账号
- 访问 [vivo开放平台](https://dev.vivo.com.cn/)
- 注册并认证

#### 上传应用
1. 登录开发者平台
2. 创建应用
3. 上传APK
4. 提交审核

### 5. 应用宝（腾讯）

#### 注册开发者账号
- 访问 [腾讯开放平台](https://open.tencent.com/)
- 注册并认证

#### 上传应用
1. 进入应用宝开发者中心
2. 创建Android应用
3. 上传APK
4. 填写资料
5. 提交审核

### 6. 其他国际商店

- **Amazon Appstore**（亚马逊）
- **Samsung Galaxy Store**（三星）
- **APKPure**（第三方）
- **F-Droid**（开源应用）

---

## 📋 上架清单

### 必备材料
- [ ] 签名的APK或AAB文件
- [ ] 应用图标（512x512）
- [ ] 应用截图（至少2张，推荐4-8张）
- [ ] 功能图片（1024x500）
- [ ] 应用简短描述（80字符）
- [ ] 应用完整描述（最多4000字符）
- [ ] 隐私政策URL
- [ ] 开发者账号（Google Play: $25）

### 可选材料
- [ ] 宣传视频
- [ ] 平板截图
- [ ] TV截图
- [ ] Wear OS截图

---

## 🔧 常见问题

### Q1: 如何更新应用？
1. 修改代码
2. 在 `android/app/build.gradle` 中增加版本号：
   ```gradle
   versionCode 2  // 从1改为2
   versionName "1.1.0"  // 从1.0.0改为1.1.0
   ```
3. 重新构建APK/AAB
4. 在Google Play Console上传新版本

### Q2: 签名密钥丢失怎么办？
⚠️ **非常严重**：无法更新应用，只能发布新应用
- 务必备份 `my-release-key.jks` 和密码
- 建议备份到云存储（加密）

### Q3: 审核被拒怎么办？
1. 查看拒绝原因
2. 修改代码或资源
3. 重新提交
4. 常见原因：
   - 隐私政策缺失
   - 截图不符合要求
   - 应用崩溃
   - 违反政策

### Q4: 如何测试发布版本？
```bash
# 安装发布版APK到设备
adb install android/app/build/outputs/apk/release/app-release.apk

# 或在Android Studio中：
# Run > Edit Configurations > Build Variant > release
```

### Q5: 如何减小APK大小？
1. 启用代码混淆（ProGuard）
2. 移除未使用的资源
3. 使用WebP格式图片
4. 启用APK拆分（按架构）
5. 使用AAB而不是APK

---

## 📚 相关文档

- [Google Play Console 帮助](https://support.google.com/googleplay/android-developer)
- [Android 应用签名](https://developer.android.com/studio/publish/app-signing)
- [Capacitor Android 部署](https://capacitorjs.com/docs/android)
- [应用上架最佳实践](https://developer.android.com/distribute/best-practices/launch)

---

## 🎯 下一步

1. ✅ 在Android Studio中测试应用
2. ⬜ 生成签名密钥
3. ⬜ 构建发布版APK/AAB
4. ⬜ 准备商店展示资源
5. ⬜ 创建Google Play开发者账号
6. ⬜ 上传到Google Play
7. ⬜ 等待审核
8. ⬜ 发布成功！🎉

---

**祝你应用上架成功！** 🚀
